# GLOSSARY — Fine-tuning / LoRA

Термины из модуля по алфавиту. Латиница — первой, затем кириллица.

---

## A

**Alpaca format**  
Формат датасета для instruction fine-tuning: три поля — `instruction` (задача), `input` (контекст), `output` (ожидаемый ответ). Стандартный выбор для single-turn задач. При форматировании оборачивается в chat template модели.

---

## B

**Baseline**  
Результат базовой модели без fine-tuning на тех же тестовых данных. Обязательная точка отсчёта: fine-tuned модель должна превосходить baseline количественно, иначе fine-tuning не имел смысла.

**bitsandbytes**  
Python библиотека для квантизации весов нейросетей. Реализует NF4 и INT8 квантизацию с GPU поддержкой. Основа QLoRA — используется Unsloth и PEFT для загрузки 4-bit моделей.

---

## C

**Chat template**  
Шаблон форматирования диалога, встроенный в токенизатор модели. Определяет как `instruction`/`input`/`output` из датасета оборачиваются в специальные токены (`<|im_start|>`, `<|im_end|>` и др.). При fine-tuning важно использовать тот же chat template что у базовой модели.

---

## D

**Data leakage**  
Проникновение тестовых данных в тренировочную выборку. При random split аугментированные вариации одного примера могут попасть в разные split'ы. Предотвращается стратификацией по `source_id`.

---

## E

**Early stopping**  
Остановка обучения когда eval_loss перестаёт улучшаться N шагов подряд. Предотвращает переобучение. Параметр `patience` (в HuggingFace — `early_stopping_patience`) определяет сколько eval шагов ждать.

**eval_loss**  
Loss на валидационной выборке, не участвующей в обучении. Падающий eval_loss — признак корректного обучения. Растущий eval_loss при падающем train_loss — переобучение.

**Exact match**  
Метрика оценки: доля примеров где предсказанный вывод совпадает с ожидаемым побайтово (после нормализации). Для structured extraction — сравнение после JSON.parse обоих значений.

---

## F

**Fine-tuning**  
Дообучение предобученной модели на специализированном датасете для адаптации поведения к конкретной задаче или домену. Изменяет веса модели. Три варианта: full fine-tuning (все веса), PEFT (подмножество параметров), LoRA/QLoRA (адаптеры низкого ранга).

**fp16 (float16)**  
16-битная точность с плавающей запятой. Используется на GTX 1660 — карта не поддерживает bfloat16. При обучении требует gradient scaling для предотвращения underflow. При нестабильности (nan loss) — уменьшить lr или добавить gradient clipping.

---

## G

**GGUF**  
Формат файла для хранения квантованных LLM весов, совместимый с llama.cpp, LM Studio, Ollama. Заменил GGML. Содержит веса + метаданные модели в одном файле. Уровни квантизации: q4_k_m, q5_k_m, q8_0, f16.

**Gradient accumulation**  
Накопление градиентов за N шагов перед обновлением весов. `gradient_accumulation_steps=4` при `batch_size=1` даёт effective batch size 4 без VRAM overhead. Позволяет обучать с большим effective batch на ограниченном VRAM.

**Gradient checkpointing**  
Техника снижения потребления VRAM: активации не сохраняются между слоями, а перевычисляются при backprop. Режим `"unsloth"` в Unsloth даёт ~30% экономию VRAM при ~20% росте времени обучения.

---

## I

**Instruction tuning**  
Тип fine-tuning: обучение модели следовать явным инструкциям в специфичном домене или формате. Датасет: пары (инструкция, ожидаемый ответ). Результат: модель предсказуемо следует инструкциям там где базовая модель вариативна.

---

## L

**LoRA (Low-Rank Adaptation)**  
PEFT метод: обновление весов аппроксимируется произведением двух малых матриц \Delta W = BA где ранг r \ll \min(d,k). Обучаются только матрицы A и B. Базовые веса заморожены. После обучения адаптер может быть смержен с базовой моделью без оверхеда при инференсе.

**lora_alpha**  
Scaling параметр LoRA: итоговый адаптер умножается на `alpha/r`. При `alpha=r` — scaling=1.0 (стандарт). При `alpha=2r` — усиленная адаптация с риском нестабильности. Не влияет на количество обучаемых параметров.

**lora_dropout**  
Dropout применяемый к LoRA адаптеру во время обучения. Регуляризация для предотвращения переобучения. Типичные значения: 0.05–0.1 для малых датасетов, 0.0 для больших.

---

## M

**Merge (LoRA merge)**  
Операция слияния обученного LoRA адаптера с базовой моделью: `W_merged = W + B×A×(alpha/r)`. Результат — полная модель без необходимости загружать адаптер отдельно. Обязателен перед экспортом в GGUF.

---

## N

**NF4 (Normal Float 4)**  
4-битный формат квантизации с неравномерной сеткой, оптимизированной под нормальное распределение весов нейросетей. Точнее INT4 при том же объёме. Используется в QLoRA через bitsandbytes.

---

## O

**Overfitting (переобучение)**  
Состояние модели: train_loss падает, eval_loss растёт. Модель запоминает тренировочные примеры вместо обобщения паттерна. При малых датасетах наступает на 3–7 эпохе. Митигация: early stopping, dropout, меньший rank, больше данных.

---

## P

**PEFT (Parameter-Efficient Fine-Tuning)**  
Семейство методов fine-tuning при которых обновляется малое подмножество параметров. LoRA — наиболее популярный PEFT метод. HuggingFace PEFT библиотека — референсная реализация.

---

## Q

**q4_k_m**  
Уровень квантизации GGUF: 4-bit с K-quant методом, medium вариант. Стандарт для production деплоя — баланс качества и размера файла. Для 2B модели ≈ 1.4 ГБ.

**QLoRA (Quantized LoRA)**  
Fine-tuning метод: базовая модель загружается в 4-bit (NF4), LoRA адаптер обучается в 16-bit поверх замороженных квантованных весов. Позволяет обучать 4B модели на GPU с 6 ГБ VRAM.

---

## R

**r (rank)**  
Ранг LoRA адаптера — размерность промежуточного пространства. Определяет количество обучаемых параметров: `2 × d × r` на слой. Малый rank (4–8): быстро, мало параметров, может не хватить. Высокий rank (64–128): риск переобучения на малых датасетах. Стартовое значение: r=16.

---

## S

**SFT (Supervised Fine-Tuning)**  
Тип fine-tuning на размеченных парах (вход, ожидаемый выход). Противопоставляется RLHF (Reinforcement Learning from Human Feedback). `SFTTrainer` из TRL библиотеки — стандартный инструмент для SFT в связке с PEFT/Unsloth.

**ShareGPT format**  
Формат датасета для multi-turn fine-tuning: поле `conversations` — массив объектов `{from: "human"|"gpt", value: "..."}`. Используется когда нужно обучить модель поддерживать диалог, а не только single-turn instruction following.

**Structural error rate**  
Метрика оценки: доля ответов модели которые не парсятся как валидный JSON (или другой ожидаемый формат). Отдельная от accuracy метрика — структурная ошибка хуже семантической: данные нельзя использовать вообще.

---

## T

**target_modules**  
Список линейных слоёв transformer-блока к которым применяется LoRA. Стандартный набор: q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj. Уменьшение набора экономит VRAM незначительно, но снижает качество адаптации.

**train_loss**  
Loss на тренировочной выборке. Должен монотонно убывать. Резкие скачки вверх — нестабильность (gradient explosion), требует gradient clipping. `nan` — overflow в fp16, требует уменьшения lr.

---

## U

**Underfitting (недообучение)**  
Состояние модели: train_loss и eval_loss падают медленно или не падают. Модель не улавливает паттерн датасета. Причины: слишком малый rank, слишком малый lr, слишком мало данных, слишком мало эпох.

**Unsloth**  
Python библиотека для ускоренного fine-tuning LLM: 2–3× быстрее стандартного HuggingFace обучения, лучшая VRAM эффективность. Поддерживает QLoRA, VLM fine-tuning (включая Qwen3.5). Версия 2026.3.x актуальна на март 2026.

---

## W

**warmup_ratio**  
Доля шагов обучения с линейно нарастающим learning rate (от 0 до целевого). Предотвращает нестабильность в начале обучения когда градиенты велики. Типичные значения: 0.03–0.1.

**weight_decay**  
L2 регуляризация весов: штраф за большие значения параметров. Снижает переобучение. Стандартное значение 0.01 работает для большинства задач.

---

## Г

**Гиперпараметры LoRA**  
Набор параметров определяющих ёмкость и поведение адаптера: `r` (rank), `lora_alpha`, `lora_dropout`, `target_modules`. Изменяются только по результатам измерения eval_loss — не интуитивно.

---

## П

**Переобучение** → см. *Overfitting*

---

## Э

**Экспорт GGUF**  
Конвертация обученной merged модели в GGUF формат для деплоя в LM Studio / Ollama. Выполняется через `model.save_pretrained_gguf()` в Unsloth с указанием уровня квантизации. Стандарт: q4_k_m.

---

*Глоссарий модуля 13. Следующий: [Модуль 14 — OOXML: raw XML внутри DOCX*](../14-ooxml/GLOSSARY.md)