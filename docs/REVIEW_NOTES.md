# REVIEW NOTES — что проверить автору

> Создан: 2026-08-06 (сессия: план A–D, блоки A, C1, C2)
> Обновлён: 2026-08-06 (вечер) — B и C1 выполнены на реальных данных CourtDesk;
> кейсы 30/31/32/34/35/38 переписаны под реальные системы (автор подтвердил:
> SEO-проект, PHP-сайт, PDFtoText; WooCommerce-каталог НЕ реальный)
> Назначение: пункты, где агент принимал решения на своё усмотрение
> или не имел данных. Автор проверяет/уточняет и вычёркивает.

---

## Блок B (модуль 12 RAG) — ВЫПОЛНЕН структурно

- [x] Кейс «RAG по судебным документам» написан структурно (архитектура, решения,
  грабли домена) на базе CourtDesk/модулей 49/15/16/18.
- [ ] ⚠️ **Метрики — плейсхолдеры `[МЕТРИКА: ...]`** (5 штук, §9): вставь реальные
  значения (recall@k, точность дат/сумм, cost/день, latency p95, доля abstain),
  либо подтверди, что их не будет (убрать плейсхолдеры).

## Блок C1 (модуль 51) — ВЫПОЛНЕН на реальных данных

- [x] Карта §9 заменена реальными 26 эндпоинтами CourtDesk (docs/API.md v0.7.1);
  §5 (success-обёртка), §8 (нет auth — LAN, ADR) приведены к реальности.
- [ ] ⚠️ Проверить: формулировки о версии API (её нет — локальный сервис) и
  ограничениях (JSON-store → SQLite) соответствуют твоему видению.
- [ ] ⚠️ `request_id` в реальном API отсутствует — в модуле помечено как
  требование для внешнего хостинга. Подтверди формулировку.

## Блок A (выполнен)

- [x] **A1. Модуль 11** — добавлен второй реальный кейс: «§8. RouterAI как fallback pool».
  Факты — из `knowledge/routerai-api.md` (грабли: error-строка, HTTP 200 для битого PDF,
  `</think>`-обрывки, ping-тест ключа). Секции 8–10 перенумерованы в 9–11.
  ⚠️ **Проверить:** формулировки кейса соответствуют реальной практике (проект PDFtoText),
  цифра «70–80% простых документов» взята из модуля 08, не выдумана.

- [x] **A2. Модуль 20** — удалена строка «Node.js 26.x Current» из таблицы версий
  (унификация под 24 LTS). ⚠️ Если Node 26 всё ещё используется в каком-то стеке —
  сообщи, вернём с пометкой.

- [x] **A3. Модуль 16** — `CURRENT_VLM_MODEL` в стеке «Реального кейса» заменён на
  «локальная VLM (LM Studio)». Плейсхолдер модели в кейсе убран (модель не влияет на
  суть кейса). ⚠️ Если хочется конкретики — подставь реальную модель в стек.

## Блок C1 (модуль 51 — API Design)

- [x] Модуль написан на домене CourtDesk: сущности из модуля 49 (courts/cases/events/
  participants), документы (16), SSRF-инцидент (47), 1С-форматы (17), кодировки
  (knowledge cp1251-gas).
- ⚠️ **ГЛАВНОЕ: карта API (раздел §9) — обобщённый прототип, НЕ реальные 26 эндпоинтов.**
  Проверь и замени на реальный список: имена ресурсов, подресурсы, RPC-операции,
  реальные коды ошибок, реальный паттерн пагинации.
- ⚠️ Проверить: «~3₽/1M входных токенов» RouterAI (взято из knowledge/routerai-api.md).
- ⚠️ Проверить: описание 1С-потребителя (регламентные задания, курсор, XLSX-выгрузки) —
  насколько совпадает с реальной интеграцией.
- ⚠️ Проверить: «Idempotency-Key TTL 24–48 ч», «limit ≤ 1000», «500 дел на пачку» —
  рабочие значения по умолчанию, подставь реальные.

## Блок C2 (модуль 52 — Resilience Patterns)

- [x] Модуль написан: таймауты, retry + jitter, Retry Storm, CB, Bulkhead, rate limit,
  Saga, Fallback. Реальный кейс — 429-шторм при сборе судебных данных.
- ⚠️ **Проверить реальный кейс (§9):** «10 воркеров, retry через 1s без jitter, блокировка
  IP на сутки» — из реального инцидента автора? Если детали отличаются (число воркеров,
  длительность бана) — поправить.
- ⚠️ Проверить: «1 req / 2 сек на домен», «CB 40% 5xx за 30 сек, HALF_OPEN 10 мин»,
  «bulkhead 3 воркера на домен» — рабочие значения, подставь реальные.
- ⚠️ Проверить: упоминание RuCaptcha как отдельного пути (капча ≠ retry) — совпадает
  с практикой?

## Блок B (модуль 12 RAG) — ждёт данных автора

- [ ] Заглушка «Реального кейса» (12-rag/README.md:675-681) ждёт: какой кейс
  (документооборот/суды, WooCommerce или Telegram) + реальные метрики. Не выдумывать.

## Блок D (этап 1 — модули 21–26, выполнен)

- [x] **21 testing** — добавлен «Реальный кейс» (тестирование PDF/VLM-пайплайна: golden-выборка 50 PDF, пороги, schema). Факты — из кейса 16 (§10) и модуля 9. ⚠️ «Каждый прод-инцидент обязан пополнять выборку» — подтверди, что практика такая.
- [x] **22 worker-threads** — добавлен «Реальный кейс» (PDF-рендер в Worker pool: 4 воркера, transferList, maxQueue 50). Факты — из 16 §10/§11. ⚠️ Подтверди: краши WASM абсорбировались Piscina-пулом, время восстановления ~500ms.
- [x] **23 rate-limiting** — добавлен «Реальный кейс» (квота как бюджет: резерв 30/70, окна RPM/RPD, очередь перед лимитером). ⚠️ Проверить: резерв 30% под критичные задачи — реальная практика или рабочее значение? «Судебный сбор: лимит на сессии + WAF-backoff» — подтверди.
- [x] **24 docker** — добавлен «Реальный кейс» (680 MB → 82 MB за счёт WASM; tini как ENTRYPOINT). Факты — из 16 §10. ⚠️ Подтверди: graceful shutdown через tini был реальным фиксом.
- [x] **25 cicd** — добавлен «Реальный кейс» (CI для курса: glossary-links + build-html, CRLF-грабли, сломанные fences). ⚠️ **ГЛАВНОЕ**: реальный CI для курса существует? Если workflow ещё нет — кейс описывает задуманное; пометь и приведи реальный.
- [x] **26 logging** — добавлен «Реальный кейс» (request_id против тикета «всё молчит»; 1С-клиент игнорировал Retry-After). ⚠️ Подтверди: инцидент «клиент ретраил через 1с, игнорируя Retry-After 60» — реальный; имена/цифры лога (request_id формат) — примерные.

## Блок D (этап 2 — модули 27–40) — не начинался

- [ ] 14 модулей (507–723 строк) — расширение до 800–1200 строк. Отдельный заход.

## Блок D (этап 2 — модули 27–40, выполнен)

Все 14 модулей: добавлены top-level «Реальный кейс» (27–38) и «Формула:» во все задачи (27–40). Объёмы: 599–767 строк (было 507–723). Пункты на проверку (кейсы построены на реальной практике автора из модулей курса и knowledge, детали обобщены):

- [ ] **27** — «SSG без фреймворка» (сборка курса build-html.mjs): подтверди, что цифры (6 MB, ~2s, 200 строк сборщика) актуальны.
- [ ] **28** — «CWV в SEO-проекте» (SerpWatcher-экосистема): проседание позиций после редизайна, INP-фиксы — реальная история? Цифры (LCP/INP Poor на PHONE) — примерные.
- [ ] **29** — «Critical CSS для PHP-сайта» (WebForge): 40 KB style.css, 7 KB critical, грабли с :root/media — подтверди.
- [ ] **30** — переписан на SEO-проект (SEO-Zavodsvay): Вебмастер API (diagnostics, recrawl, HOST_NOT_LOADED, NO_METRIKA_COUNTER_CRAWL_ENABLED), разметка в шаблоне. Факты — из knowledge/yandex-seo-services.md. ⚠️ Подтверди: сайт/цикл соответствует практике.
- [ ] **31** — переписан на PHP-сайт (услуги, карточки, формы заявок): 60% мобильного трафика, CQ-блоки — подтверди.
- [ ] **32** — переписан на PHP-сайт: focus trap в модалке звонка, форма заявки (aria-live ошибок), контраст подвала 3.1:1 — реальные находки аудита?
- [ ] **33** — «LoAF вместо догадок» (связка с кейсом 28): фильтр каталога 600ms, виджет 450ms — реальные цифры LoAF-сбора?
- [ ] **34** — переписан на PHP-сайт (лента статей, галерея): rootMargin 200px, буфер 3 карточки — подтверди.
- [ ] **35** — переписан на PDFtoText/VLM: tile-экономика, 1000px/1536px, CMYK-грабли — подтверди.
- [ ] **36** — «CRP PHP-сайта»: 2.9s→1.4s FCP, порядок фиксов — реальные замеры? (последовательность шагов — обобщение модели CRP).
- [ ] **37** — «WASM-память, которую GC не видит» (PDFium, модуль 16): 1.8 GB RSS, ~10–50 MB на документ — подтверди цифры.
- [ ] **38** — переписан на PHP-сайт: nginx Cache-Control + rsync-деплой с --delete, грабли «горячей правки» — подтверди.
- [ ] **39/40** — только «Формула:» в задачах, контент не менялся.

## Технические примечания

- После правок переконвертировать изменённые файлы в LF (иначе git diff покажет весь файл).
- Сборка: `cd scripts && node glossary-links.mjs && node build-html.mjs` — 52/52.
- Автор коммитит сам; в этой сессии коммиты не делались.

---

## Аудит точности и свежести — 2026-09-22 (скрипт + ручная сверка, поиск в вебе недоступен)

Метод: скрипт по всем 52 главам (даты таблиц, плейсхолдеры CURRENT_*, баланс fences, ссылки глоссария, реестр сборки, grep stale-версий) + ручная проверка подозрений + сверка с живой практикой сентября (CaseCore: TS 7.0.2, zod 4.4.3, PG 18.6, BullMQ 6.2.2, Redis 8, Caddy 2.11.4, Docker 29.1). Веб-поиск для перепроверки версий был недоступен (транспортная ошибка провайдера) — основанием свежести взята собственная продакшен-практика, а не статьи.

### Структура — чисто (проверено скриптом, чинить нечего)

- Битых fences нет: нечётные счётчики в 06 и 25 — ложная тревога наивного подсчёта (упоминания fences в тексте); построчный подсчёт чётен (94 и 48), сборка рендерит.
- Битых ссылок глоссария — 0, реестр сборки полон (52/52, пропусков нет).
- Устаревших версий CI-экшенов и ubuntu-образов в примерах — 0 (один случай ниже — починен).
- Упоминания Python 3.10/3.11 и FID — исторические, корректны.

### Починено в ходе аудита (без участия автора)

- [x] **40** — пример workflow был на checkout v4 / setup-node v4 / node 22 / ubuntu-latest; приведён к стандарту модуля 25 (v5 / v6 / 24 / ubuntu-24.04).
- [x] **46** — в таблице версий неанглийская строка вместо статуса; заменена на stable.

### Свежесть против живой практики — ждёт автора (по приоритету)

Правило от автора 2026-09-22: в разработке всегда последние стабильные, эталон — CaseCore. Проверено вживую: TS 7.0.2 latest, zod 4.5.x latest, PG 18.6 current, BullMQ 6.3.x current, Node 24 LTS / 26 Current / 25 EOL. Закрытое ниже — по этому правилу.

Средний приоритет (таблицы отстают от продакшена на поколение):

- [x] **02** — закрыто 2026-09-22: добавлена строка 7.0.2 (npm latest, Go-порт), 6.0.3 — предыдущий стабильный и мост тулинга.
- [x] **07** — закрыто 2026-09-22: zod поднят до 4.x (практика на 4.4.3), v3 — legacy.
- [x] **49/12/18/51/52** — закрыто 2026-09-22: PostgreSQL поднят до 18.x во всех пяти.
- [x] **18** — закрыто 2026-09-22: BullMQ поднят до 6.x, Redis до 8.x; грабли мажора (repeat→Job Schedulers, ioredis явно) — в сравнении инструментов.
- [x] **52** — закрыто 2026-09-22: Redis поднят до 8.x.
- [ ] **12** — 4–5 плейсхолдеров метрик (уже open-проблема 1 в контексте). Вставить значения или убрать.
- [x] **01** — закрыто 2026-09-22: Node 25 — EOL (нечётные без LTS-фазы), проверено по расписанию релизов.
- [ ] **11** — LM Studio 0.4.8 и квоты Groq датированы мартом; квоты меняются часто. Перепроверить цифры.

Низкий приоритет (проверить при следующем проходе):

- [ ] **42** — A2A v0.3.x / a2a-js 0.2.x, экосистема быстрая; перепроверить дату.
- [ ] **32** — NVDA 2024.x, вероятно вышел 2025.x.
- [ ] **26** — pino-http 10.x в рекомендации, а практика его снесла за гигиеной; подтвердить позицию курса.
- [ ] **44** — Playwright широким пином 1.5x, ок; при случае сузить.
- [ ] Процесс: даты «Проверено» есть только у 06, 08, 10, 41–52; у 01–05, 07, 09, 11–40 явной даты нет, часть таблиц — март 2026 (полгода). При следующем касании каждого модуля проставлять дату.
- [ ] Объёмы 41–52 ниже нормы 800–1200 (принято в августе как плотные); не дефект, зафиксировано для истории.
