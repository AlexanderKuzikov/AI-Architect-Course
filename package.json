{
  "name": "ai-architect-course",
  "private": true,
  "description": "AI Architect Course — build tooling",
  "scripts": {
    "build": "cd scripts && npm install --silent && node glossary-links.mjs && node build-html.mjs"
  }
}
